---
phase: 02-package-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/tsdown.config.ts
  - packages/shared/vitest.config.ts
  - packages/shared/src/**
  - packages/queue/package.json
  - packages/queue/tsconfig.json
  - packages/queue/tsdown.config.ts
  - packages/queue/src/index.ts
  - packages/agent/package.json
  - packages/agent/tsconfig.json
  - packages/agent/tsdown.config.ts
  - packages/agent/vitest.config.ts
  - packages/agent/src/**
autonomous: true
requirements:
  - PACK-01
  - PACK-02
  - PACK-03
  - PACK-04

must_haves:
  truths:
    - "packages/shared/ contains all 63 source files plus 17 YAML spec files with package name @robin/shared"
    - "packages/queue/ contains BullMQ abstraction with package name @robin/queue and workspace:* dep on @robin/shared"
    - "packages/agent/ contains all agents and stages with package name @robin/agent and workspace:* dep on @robin/shared"
    - "Agent vitest alias points to ../shared/src/index.ts (not ../../packages/shared/src/index.ts)"
    - "No node_modules/ or dist/ directories were copied from source"
  artifacts:
    - path: "packages/shared/package.json"
      provides: "@robin/shared package identity"
      contains: "@robin/shared"
    - path: "packages/shared/src/prompts/specs/"
      provides: "17 YAML prompt spec files"
    - path: "packages/queue/package.json"
      provides: "@robin/queue package identity"
      contains: "@robin/queue"
    - path: "packages/agent/package.json"
      provides: "@robin/agent package identity"
      contains: "@robin/agent"
    - path: "packages/agent/vitest.config.ts"
      provides: "Corrected vitest alias for new layout"
      contains: "../shared/src/index.ts"
  key_links:
    - from: "packages/agent/package.json"
      to: "packages/shared/"
      via: "workspace:* dependency"
      pattern: "\"@robin/shared\": \"workspace:\\*\""
    - from: "packages/queue/package.json"
      to: "packages/shared/"
      via: "workspace:* dependency"
      pattern: "\"@robin/shared\": \"workspace:\\*\""
    - from: "packages/agent/tsconfig.json"
      to: "packages/shared/dist/index.d.ts"
      via: "tsconfig paths"
      pattern: "\"@robin/shared\": \\[\"../shared/dist/index.d.ts\"\\]"
---

<objective>
Migrate the three workspace packages (@robin/shared, @robin/queue, @robin/agent) from the robin-fullstack source repo into packages/ in the new robin workspace.

Purpose: Place all workspace packages in the correct locations with correct identities and cross-references so Phase 3 (server migration) can depend on them and Phase 4 can verify builds.

Output: Three fully populated package directories under packages/ with one path fix in agent's vitest config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-package-migration/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate @robin/shared package (PACK-03)</name>
  <files>packages/shared/package.json, packages/shared/tsconfig.json, packages/shared/tsdown.config.ts, packages/shared/vitest.config.ts, packages/shared/src/**</files>
  <read_first>
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/shared/package.json
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/shared/tsconfig.json
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/shared/tsdown.config.ts
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/shared/vitest.config.ts
  </read_first>
  <action>
Copy the @robin/shared package from the source repo to packages/shared/ in the destination. This package has NO cross-package dependencies so it copies verbatim with zero modifications.

Steps:
1. Create the destination directory: `mkdir -p packages/shared`
2. Copy source files excluding node_modules and dist:
   ```bash
   rsync -av --exclude='node_modules' --exclude='dist' \
     /Users/apple/srv/withrobinhq/robin-fullstack/packages/shared/ \
     /Users/apple/srv/withrobinhq/robin/packages/shared/
   ```
3. Verify the package.json has name `@robin/shared` (should already be correct -- do NOT modify)
4. Verify tsconfig.json extends `../../tsconfig.base.json` (should already be correct -- do NOT modify)
5. Verify tsdown.config.ts has the `copy` directive for YAML specs: `{ from: 'src/prompts/specs', to: 'dist/prompts' }` (should already be correct -- do NOT modify)
6. Verify all 17 YAML spec files exist under `packages/shared/src/prompts/specs/`

CRITICAL: Do NOT modify any files. This is a verbatim copy. The tsconfig paths, tsdown config, and vitest config are all correct as-is for the new layout.
  </action>
  <verify>
    <automated>test -f /Users/apple/srv/withrobinhq/robin/packages/shared/package.json && grep -q '"@robin/shared"' /Users/apple/srv/withrobinhq/robin/packages/shared/package.json && test $(find /Users/apple/srv/withrobinhq/robin/packages/shared/src/prompts/specs -name "*.yaml" | wc -l | tr -d ' ') -eq 17 && test ! -d /Users/apple/srv/withrobinhq/robin/packages/shared/node_modules && test ! -d /Users/apple/srv/withrobinhq/robin/packages/shared/dist && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/package.json exists and contains `"name": "@robin/shared"`
    - packages/shared/tsconfig.json exists and contains `"extends": "../../tsconfig.base.json"`
    - packages/shared/tsdown.config.ts exists and contains `copy:`
    - packages/shared/src/prompts/specs/ contains exactly 17 .yaml files
    - packages/shared/src/index.ts exists
    - No node_modules/ or dist/ directories under packages/shared/
  </acceptance_criteria>
  <done>@robin/shared is fully migrated to packages/shared/ with all 63 source files and 17 YAML spec files, no stale artifacts</done>
</task>

<task type="auto">
  <name>Task 2: Migrate @robin/queue and @robin/agent packages (PACK-01, PACK-02)</name>
  <files>packages/queue/package.json, packages/queue/tsconfig.json, packages/queue/tsdown.config.ts, packages/queue/src/index.ts, packages/agent/package.json, packages/agent/tsconfig.json, packages/agent/tsdown.config.ts, packages/agent/vitest.config.ts, packages/agent/src/**</files>
  <read_first>
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/queue/package.json
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/queue/tsconfig.json
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/agent/package.json
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/agent/tsconfig.json
    /Users/apple/srv/withrobinhq/robin-fullstack/packages/agent/vitest.config.ts
  </read_first>
  <action>
Copy both @robin/queue and @robin/agent from the source repo. Both depend on @robin/shared via workspace:*. Queue copies verbatim. Agent requires ONE change to vitest.config.ts.

Steps for @robin/queue:
1. `mkdir -p packages/queue`
2. Copy excluding node_modules and dist:
   ```bash
   rsync -av --exclude='node_modules' --exclude='dist' \
     /Users/apple/srv/withrobinhq/robin-fullstack/packages/queue/ \
     /Users/apple/srv/withrobinhq/robin/packages/queue/
   ```
3. Verify package.json has name `@robin/queue` and dependency `"@robin/shared": "workspace:*"` -- do NOT modify
4. Verify tsconfig.json extends `../../tsconfig.base.json` and has paths entry `"@robin/shared": ["../shared/dist/index.d.ts"]` -- do NOT modify

Steps for @robin/agent:
1. `mkdir -p packages/agent`
2. Copy excluding node_modules and dist:
   ```bash
   rsync -av --exclude='node_modules' --exclude='dist' \
     /Users/apple/srv/withrobinhq/robin-fullstack/packages/agent/ \
     /Users/apple/srv/withrobinhq/robin/packages/agent/
   ```
3. Verify package.json has name `@robin/agent` and dependency `"@robin/shared": "workspace:*"` -- do NOT modify
4. Verify tsconfig.json extends `../../tsconfig.base.json` and has paths for `@robin/shared` and `@robin/queue` -- do NOT modify

ONE REQUIRED CHANGE in packages/agent/vitest.config.ts:
- Change the alias path from `../../packages/shared/src/index.ts` to `../shared/src/index.ts`
- The old path was correct for the source repo layout (robin-fullstack/packages/agent/ -> robin-fullstack/packages/shared/)
- The new path is correct for the destination layout (robin/packages/agent/ -> robin/packages/shared/)
- The updated file should be:
```typescript
import { defineConfig } from 'vitest/config'
import { resolve } from 'path'

export default defineConfig({
  test: {
    environment: 'node',
    globals: false,
  },
  resolve: {
    alias: {
      '@robin/shared': resolve(__dirname, '../shared/src/index.ts'),
    },
  },
})
```

Do NOT modify any other files. All tsconfig.json paths are already correct for the new layout (verified: `../shared/dist/index.d.ts` is the same relative path in both source and destination).
  </action>
  <verify>
    <automated>grep -q '"@robin/queue"' /Users/apple/srv/withrobinhq/robin/packages/queue/package.json && grep -q '"@robin/shared": "workspace:\*"' /Users/apple/srv/withrobinhq/robin/packages/queue/package.json && grep -q '"@robin/agent"' /Users/apple/srv/withrobinhq/robin/packages/agent/package.json && grep -q '"@robin/shared": "workspace:\*"' /Users/apple/srv/withrobinhq/robin/packages/agent/package.json && grep -q '../shared/src/index.ts' /Users/apple/srv/withrobinhq/robin/packages/agent/vitest.config.ts && ! grep -q '../../packages/shared/src/index.ts' /Users/apple/srv/withrobinhq/robin/packages/agent/vitest.config.ts && test ! -d /Users/apple/srv/withrobinhq/robin/packages/queue/node_modules && test ! -d /Users/apple/srv/withrobinhq/robin/packages/agent/node_modules && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - packages/queue/package.json contains `"name": "@robin/queue"`
    - packages/queue/package.json contains `"@robin/shared": "workspace:*"`
    - packages/queue/src/index.ts exists
    - packages/agent/package.json contains `"name": "@robin/agent"`
    - packages/agent/package.json contains `"@robin/shared": "workspace:*"`
    - packages/agent/vitest.config.ts contains `../shared/src/index.ts` (NOT `../../packages/shared/src/index.ts`)
    - packages/agent/src/index.ts exists
    - packages/agent/src/agents/ directory exists
    - packages/agent/src/stages/ directory exists
    - No node_modules/ or dist/ directories under packages/queue/ or packages/agent/
  </acceptance_criteria>
  <done>@robin/queue and @robin/agent are fully migrated with correct workspace:* dependencies and the vitest alias fix applied</done>
</task>

<task type="auto">
  <name>Task 3: Verify cross-package structure and workspace:* references (PACK-04)</name>
  <files></files>
  <read_first>
    /Users/apple/srv/withrobinhq/robin/packages/agent/package.json
    /Users/apple/srv/withrobinhq/robin/packages/queue/package.json
    /Users/apple/srv/withrobinhq/robin/packages/shared/package.json
    /Users/apple/srv/withrobinhq/robin/packages/agent/tsconfig.json
    /Users/apple/srv/withrobinhq/robin/packages/queue/tsconfig.json
  </read_first>
  <action>
Verify the complete package migration by checking all cross-references resolve to real paths.

Verification steps (all read-only, no modifications):

1. Confirm all three package.json files have correct names:
   - packages/shared/package.json -> `"name": "@robin/shared"`
   - packages/queue/package.json -> `"name": "@robin/queue"`
   - packages/agent/package.json -> `"name": "@robin/agent"`

2. Confirm workspace:* dependencies are declared:
   - packages/queue/package.json has `"@robin/shared": "workspace:*"`
   - packages/agent/package.json has `"@robin/shared": "workspace:*"`
   - packages/shared/package.json has NO workspace:* dependencies (it is the leaf)

3. Confirm tsconfig paths point to real relative locations:
   - packages/agent/tsconfig.json `"@robin/shared": ["../shared/dist/index.d.ts"]` -- verify ../shared/ exists
   - packages/agent/tsconfig.json `"@robin/queue": ["../queue/dist/index.d.ts"]` -- verify ../queue/ exists
   - packages/queue/tsconfig.json `"@robin/shared": ["../shared/dist/index.d.ts"]` -- verify ../shared/ exists

4. Confirm no stale source artifacts:
   - No node_modules/ in any package directory
   - No dist/ in any package directory

5. Count total files migrated to confirm completeness:
   - packages/shared/src/ should have ~63 files (including YAML)
   - packages/agent/src/ should have ~37 files
   - packages/queue/src/ should have 1 file (index.ts)

Report counts. If any count is zero or wildly different from expected, flag as an issue.
  </action>
  <verify>
    <automated>cd /Users/apple/srv/withrobinhq/robin && SHARED_COUNT=$(find packages/shared/src -type f | wc -l | tr -d ' ') && AGENT_COUNT=$(find packages/agent/src -type f | wc -l | tr -d ' ') && QUEUE_COUNT=$(find packages/queue/src -type f | wc -l | tr -d ' ') && YAML_COUNT=$(find packages/shared/src/prompts/specs -name "*.yaml" | wc -l | tr -d ' ') && echo "shared=$SHARED_COUNT agent=$AGENT_COUNT queue=$QUEUE_COUNT yaml=$YAML_COUNT" && test $SHARED_COUNT -gt 50 && test $AGENT_COUNT -gt 30 && test $QUEUE_COUNT -eq 1 && test $YAML_COUNT -eq 17 && test -d packages/shared && test -d packages/agent && test -d packages/queue && echo "ALL COUNTS PASS" || echo "COUNT CHECK FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/ contains 50+ files (types, prompts, utilities)
    - packages/agent/src/ contains 30+ files (agents, stages, modules)
    - packages/queue/src/ contains exactly 1 file (index.ts)
    - packages/shared/src/prompts/specs/ contains exactly 17 YAML files
    - All three package.json files have correct @robin/* names
    - workspace:* deps in agent and queue point to @robin/shared
    - No node_modules/ or dist/ in any migrated package
  </acceptance_criteria>
  <done>All three packages verified in place with correct identities, correct cross-references, correct file counts, and no stale artifacts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source repo -> destination repo | Files copied from external repo; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | File copy from source repo | accept | Source repo is local, trusted, developer-controlled. No network fetch involved. |
| T-02-02 | Information Disclosure | Copied files may contain secrets | accept | Migration of existing code -- secrets management is unchanged from source repo. No new exposure surface. |
</threat_model>

<verification>
1. All three package directories exist under packages/ with correct package.json names
2. YAML spec files: `find packages/shared/src/prompts/specs -name "*.yaml" | wc -l` returns 17
3. Vitest alias fix: `grep '../shared/src/index.ts' packages/agent/vitest.config.ts` matches
4. No stale artifacts: no node_modules/ or dist/ in any package directory
5. workspace:* deps: grep confirms @robin/shared workspace:* in agent and queue package.json
</verification>

<success_criteria>
- packages/shared/ contains @robin/shared with all source files and 17 YAML specs
- packages/queue/ contains @robin/queue with BullMQ abstraction and workspace:* dep
- packages/agent/ contains @robin/agent with all agents/stages and corrected vitest alias
- All workspace:* cross-references point to packages that exist in the workspace
- Zero files modified from source except agent/vitest.config.ts (one path fix)
</success_criteria>

<output>
After completion, create `.planning/phases/02-package-migration/02-01-SUMMARY.md`
</output>
