---
phase: 04-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - robin/package.json
  - packages/shared/package.json
  - packages/queue/package.json
  - packages/agent/package.json
  - packages/shared/tsconfig.json
  - packages/queue/tsconfig.json
  - packages/agent/tsconfig.json
  - robin/tsconfig.json
  - tsconfig.base.json
autonomous: true
requirements: [VERI-01, VERI-02, VERI-03]

must_haves:
  truths:
    - "pnpm install completes with exit code 0 and no unresolved peer dependency errors"
    - "tsc --noEmit passes with zero errors across the full workspace"
    - "pnpm run build succeeds for @robin/shared, @robin/queue, @robin/agent, and @robin/server"
  artifacts:
    - path: "pnpm-lock.yaml"
      provides: "Resolved dependency lockfile"
    - path: "packages/shared/dist/"
      provides: "Built shared package output"
    - path: "packages/queue/dist/"
      provides: "Built queue package output"
    - path: "packages/agent/dist/"
      provides: "Built agent package output"
  key_links:
    - from: "packages/agent/package.json"
      to: "@robin/shared"
      via: "workspace:* dependency"
      pattern: '"@robin/shared":\\s*"workspace:\\*"'
    - from: "packages/queue/package.json"
      to: "@robin/shared"
      via: "workspace:* dependency"
      pattern: '"@robin/shared":\\s*"workspace:\\*"'
    - from: "robin/package.json"
      to: "@robin/agent, @robin/queue, @robin/shared"
      via: "workspace:* dependencies"
      pattern: '"@robin/(agent|queue|shared)":\\s*"workspace:\\*"'
---

<objective>
Verify the full workspace installs, compiles, and builds cleanly after phases 1-3 migration.

Purpose: Confirm the migration is complete and the monorepo is in a working state with no broken dependencies, type errors, or build failures.
Output: Clean install, zero TypeScript errors, successful builds for all workspace packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-setup/01-01-SUMMARY.md
@.planning/phases/02-package-migration/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify pnpm install and fix dependency issues</name>
  <files>package.json, robin/package.json, packages/shared/package.json, packages/queue/package.json, packages/agent/package.json, pnpm-lock.yaml</files>
  <read_first>package.json, robin/package.json, packages/shared/package.json, packages/queue/package.json, packages/agent/package.json, pnpm-workspace.yaml</read_first>
  <action>
Run `pnpm install` at the repo root. Examine the output for:
1. Missing dependency errors — add any missing packages to the correct workspace package
2. Peer dependency warnings that indicate version mismatches — resolve by adding the peer dep or adjusting version ranges
3. Workspace resolution failures — ensure all `workspace:*` references point to packages that exist in pnpm-workspace.yaml

If `pnpm install` fails, read the error output carefully. Common fixes:
- Missing package: `pnpm add <pkg> --filter <workspace-name>`
- Workspace link failure: verify package name in the target's package.json matches the dependency reference
- Engine mismatch: check node/pnpm version constraints

After fixing, re-run `pnpm install` until it exits cleanly with code 0.

VERI-01: This task directly satisfies the requirement that `pnpm install` succeeds.
  </action>
  <verify>
    <automated>cd /Users/apple/srv/withrobinhq/robin && pnpm install 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
- `pnpm install` exits with code 0
- No "ERR_PNPM" errors in output
- No "Missing peer dependency" errors (warnings acceptable if non-breaking)
- All workspace:* links resolve (verify with `pnpm ls --depth 0 -r 2>&1 | grep -c "@robin/"` showing at least 3 packages)
  </acceptance_criteria>
  <done>pnpm install completes successfully, all workspace packages are linked, lockfile is up to date</done>
</task>

<task type="auto">
  <name>Task 2: Fix TypeScript compilation errors and verify builds</name>
  <files>packages/shared/tsconfig.json, packages/queue/tsconfig.json, packages/agent/tsconfig.json, robin/tsconfig.json, tsconfig.base.json</files>
  <read_first>tsconfig.base.json, packages/shared/tsconfig.json, packages/queue/tsconfig.json, packages/agent/tsconfig.json, robin/tsconfig.json</read_first>
  <action>
Run TypeScript compilation check across the workspace and fix all errors:

Step 1 — Run `pnpm run typecheck` (which runs `turbo run typecheck`). Capture the full output.

Step 2 — If there are TypeScript errors, categorize them:
- **Missing type declarations**: Install `@types/*` packages in the correct workspace package using `pnpm add -D @types/xxx --filter @robin/pkgname`
- **Import resolution errors (Cannot find module)**: Check tsconfig.json paths, ensure `moduleResolution: "NodeNext"` is set, verify file extensions in imports
- **Type mismatches**: Fix the source code type annotations
- **Missing exports**: Add missing exports to package index files
- **tsconfig extends errors**: Ensure each package's tsconfig.json extends `../../tsconfig.base.json` (or `../tsconfig.base.json` for robin/)

Step 3 — After fixing type errors, run `pnpm run build` to build all packages. The build order (via turbo) is: shared -> queue -> agent -> server. Fix any build failures:
- **tsdown errors**: Check tsdown.config.ts or build script in each package
- **Missing dist output**: Ensure build script produces files in dist/
- **Cross-package build failures**: Ensure dependent packages build before consumers (turbo handles this via `dependsOn: ["^build"]`)

Step 4 — Re-run both `pnpm run typecheck` and `pnpm run build` to confirm zero errors.

VERI-02: typecheck passing satisfies the requirement that tsc --noEmit passes.
VERI-03: build passing satisfies the requirement that all workspace packages build.
  </action>
  <verify>
    <automated>cd /Users/apple/srv/withrobinhq/robin && pnpm run typecheck 2>&1; echo "TYPECHECK_EXIT:$?" && pnpm run build 2>&1; echo "BUILD_EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
- `pnpm run typecheck` exits with code 0 and output contains no "error TS" lines
- `pnpm run build` exits with code 0
- `ls packages/shared/dist/ packages/queue/dist/ packages/agent/dist/` shows output files in each
- No "Cannot find module" errors remain in typecheck output
  </acceptance_criteria>
  <done>TypeScript compiles with zero errors across all workspace packages, and all packages build successfully producing dist/ output</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification phase — no new code, no new inputs, no runtime behavior changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | N/A | N/A | accept | Verification phase only runs build tools on migrated code — no new attack surface introduced |
</threat_model>

<verification>
1. `pnpm install` exits 0 — no missing or unresolved dependencies
2. `pnpm run typecheck` exits 0 — zero TypeScript errors
3. `pnpm run build` exits 0 — all packages produce dist/ output
4. `pnpm ls -r --depth 0` shows @robin/shared, @robin/queue, @robin/agent (and @robin/server if robin/ exists) all linked
</verification>

<success_criteria>
- VERI-01: `pnpm install` succeeds with exit code 0
- VERI-02: `tsc --noEmit` (via `pnpm run typecheck`) passes with zero errors
- VERI-03: All workspace packages build successfully (dist/ directories populated)
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification/04-01-SUMMARY.md`
</output>
